$(document).ready(function(){
    // Sidebar javascript
    $('.sidenav').sidenav({edge: "right"});
    // Accordian tasks
    $('.collapsible').collapsible();
    // Tooltipped note on hover
    $('.tooltipped').tooltip();
  });


